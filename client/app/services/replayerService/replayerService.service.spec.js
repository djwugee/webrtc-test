'use strict';

describe('Service: replayerService', function () {

  // load the service's module
  beforeEach(module('webrtcTestApp'));

  // instantiate service
  var replayerService;
  beforeEach(inject(function (_replayerService_) {
    replayerService = _replayerService_;
  }));
/*
  it('should do something', function () {
    expect(!!replayerService).toBe(true);
  });
*/

});
