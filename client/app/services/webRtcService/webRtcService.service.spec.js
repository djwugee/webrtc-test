'use strict';

describe('Service: webRtcService', function () {

  // load the service's module
  beforeEach(module('webrtcTestApp'));

  // instantiate service
  var webRtcService;
  beforeEach(inject(function (_webRtcService_) {
    webRtcService = _webRtcService_;
  }));

/*
  it('should do something', function () {
    expect(!!webRtcService).toBe(true);
  });
*/
});
