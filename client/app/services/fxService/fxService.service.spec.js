'use strict';

describe('Service: fxService', function () {

  // load the service's module
  beforeEach(module('webrtcTestApp'));

  // instantiate service
  var fxService;
  beforeEach(inject(function (_fxService_) {
    fxService = _fxService_;
  }));

/*
  it('should do something', function () {
    expect(!!webRtcService).toBe(true);
  });
*/
});
