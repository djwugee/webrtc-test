'use strict';

describe('Service: synthService', function () {

  // load the service's module
  beforeEach(module('webrtcTestApp'));

  // instantiate service
  var synthService;
  beforeEach(inject(function (_synthService_) {
    synthService = _synthService_;
  }));
/*
  it('should do something', function () {
    expect(!!synthService).toBe(true);
  });
*/
});
