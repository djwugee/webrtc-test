'use strict';

describe('Service: midiService', function () {

  // load the service's module
  beforeEach(module('webrtcTestApp'));

  // instantiate service
  var midiService;
  beforeEach(inject(function (_midiService_) {
    midiService = _midiService_;
  }));
/*
  it('should do something', function () {
    expect(!!midiService).toBe(true);
  });
*/
});
